{
  "name": "$project_name",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predev-start": "npm run build",
    "dev-start": "node dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^2.15.1"
  },
  "devDependencies": {
    "@types/node": "^13.13.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
